import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Caso Studio: RifacciamoCasa | EdilManager24",
  description: "Scopri come RifacciamoCasa ha recuperato il 15% di marginalità netta in 6 mesi grazie a EdilManager24."
};

export default function CaseStudyLayout({ children }: { children: React.ReactNode }) {
  return children;
}

